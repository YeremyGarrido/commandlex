# wrangler.toml — usado para preview local con `npm run cf:preview`
# El deploy en producción lo maneja Cloudflare Pages desde el dashboard.
name = "commandlex"
compatibility_date = "2024-12-30"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = ".open-next/assets"
binding = "ASSETS"
